services:
  efuse-app:
    build:
      context: .
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGO_URI: mongodb://db/efuse
    depends_on:
      - mongodb
    restart: always

  db:
    image: mongo:5
